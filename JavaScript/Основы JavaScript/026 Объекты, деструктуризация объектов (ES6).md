
## Удаление свойства из объекта

```js
'use strict';

const options = {
	name: 'test',
	width: 1024,
	height: 10124,
	colors: {
		border: 'black',
		bg: 'red'
	}
};

console.log(options.name);

delete options.name;
console.log(options);
```
